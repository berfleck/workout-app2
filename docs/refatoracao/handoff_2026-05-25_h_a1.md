# Handoff — Micro-frente H-A1 (âncoras obrigatórias de subregião no CSP)

**Sessão**: 2026-05-25 — Bloco 2.5 do roadmap CSP (pré-requisito da Frente E.1)

**Leitura preparatória (na ordem)**:
1. `docs/refatoracao/norte.md` — princípios, especialmente Seção 4 (anti-padrão: "centralidade clínica vem de tag curada, não emerge do banco" — H-A1 é literalmente a declaração disso) e Seção 5 (trade-offs).
2. `docs/refatoracao/roadmap_csp.md` — Bloco 2.5 é o escopo. Frente E.0 (Bloco 2) concluída em 2026-05-24; achados que motivam H-A1 estão nos bullets desse bloco.
3. `docs/refatoracao/catalogo_constraints.md` — **seção H-A1** (escopo ROTINA, logo após H-R1) é a spec executável. Ler também H-R1, H-T4 e H-cargas pra entender o padrão de graceful degradation a espelhar.
4. `docs/refatoracao/logs/frente_e0_harness_comparativo.md` — log da E.0 com diagnóstico do bug + causa raiz (sondagem 50 runs).
5. `docs/refatoracao/relatorios/E0_2026-05-24.md` — números pré-H-A1 (baseline pra comparar pós-fix).
6. `gerador_treino.py:159-194` — `ANCORAS_POR_SUBREGIAO` (fonte canônica única, **não duplicar no CSP** — importar).
7. `gerador_treino.py:_decompor_demanda_subregiao` + `_quotas_de_subregiao` — referência conceitual do que o antigo faz imperativamente; H-A1 substitui declarativamente.

**Objetivo desta sessão**:
Implementar H-A1 hard no `gerador_csp.py`: quando demanda é `("subregiao", "X", qtd)` com qtd≥1 e X tem entrada em `ANCORAS_POR_SUBREGIAO`, exigir ≥1 slot da rotina com `padrao == âncora.padrao` pra cada âncora `obrigatoria=True`. Graceful degradation espelha H-R1/H-T4 (pula constraint quando pool não tem candidato pós-H-P1, marca `degraded=True`). Validar com sondagem ABC Day A antes/depois (esperado: `ombro_composto` de 0% pra ≥1 por rotina) + re-rodar harness comparativo E.0 (esperado: violação de âncoras obrigatórias cai pra 0% no CSP).

**Decisões já fechadas (não reabrir sem motivo forte)**:
- **Branch nova** `micro-h-a1` a partir de `main` (disciplina de merge: 1 branch ativa por vez; main inclui Frente E.0).
- **Spec é a entrada H-A1 do catálogo** — não inventar variações.
- **NÃO ativa em demanda nível padrão** (`("padrao", "Y", qtd)`): usuário foi específico, respeitar.
- **NÃO incluir distribuição entre não-obrigatórias** (pesos 1/2/3 do antigo) — isso é S-A1 futuro, fora do escopo H-A1. Implementar só obrigatoriedade.
- **Cross-treino**: obrigatoriedade vale na rotina inteira (≥1 slot global na soma dos treinos), não por treino. Espelha `_distribuir_quotas_entre_treinos` do antigo conceitualmente.
- **`ANCORAS_POR_SUBREGIAO` permanece em `gerador_treino.py`** — importar via `from gerador_treino import ANCORAS_POR_SUBREGIAO` (mesmo padrão de `PADRAO_PARA_SUBREGIAO`, `GRUPO_MUSCULAR_PADRAO`, `SUBREGIOES_LATERALIDADE_HARD`).
- **Resultado dict ganha chave `h_a1_aplicadas`** análoga a `h_r1_aplicadas`: lista de `{subregiao, padrao_obrigatorio, n_termos, degraded}`.
- **Carve-outs do antigo** (`SUBREGIOES_CARVE_OUT_QUOTAS`) e ombro vaga-única (`SUBREGIOES_SORTEIO_VAGA_UNICA`) ficam fora do H-A1 — são distribuição/sorteio, não obrigatoriedade. Migram em frente própria depois (provavelmente S-A1).
- **Gate**: pytest baseline (285+1) + harness antigo 16/16 OK + harness comparativo E.0 rodado novamente com relatório `E0_<data>_pos_h_a1.md` mostrando colapso das violações de âncora obrigatória.

**Pontos de decisão pendentes (perguntar antes de codar)**:
1. **Conflito de cardinalidade quando `qtd < n_obrigatórias`**: caso real é `bracos(1)` — só 1 vaga, mas tanto `biceps` quanto `triceps` são obrigatórias na subregião. Opções: (a) degraded automático com aviso (mantém viabilidade); (b) inviabilizar e exigir cycle/sorteio externo; (c) escolher 1 obrigatória por seed (espelha `_distribuir_quotas_entre_treinos` quando há disputa). Antigo recai em alguma decisão imperativa; vale ver qual no `_decompor_demanda_subregiao`.
2. **Interação com H-R1**: em peito, `empurrar_compostos` é âncora obrigatória de H-A1 E é o eixo composto exigido por H-R1 quando ≥2 slots. Hipótese: as duas constraints convivem (H-R1 mais estrita exige `purpose==compound` e ≥2 slots; H-A1 só exige `padrao==X` e ≥1 slot). Confirmar antes de codar que não há sobreposição patológica (ex: dupla penalty, dupla degraded).
3. **Demanda nível `regiao`** (ex: `("regiao", "upper", 3)`): expande pra padrões via cycling. H-A1 ativa pras subregiões cobertas pela região? Provavelmente NÃO — slot sem subregião determinística pré-solver (mesma regra do H-R1 — ver Seção H-R1 do catálogo). Confirmar.
4. **Adapter `app_flask.py`**: `treino_regerar` precisa passar algo novo? Provavelmente não — H-A1 é interno ao motor, sem novo argumento na API pública (`gerar_rotina_csp` / `gerar_treino_csp`).

**Restrições / não-fazer**:
- NÃO tocar `gerador_treino.py` (motor antigo).
- NÃO mexer no XLSX (banco) ou em `ANCORAS_POR_SUBREGIAO` (que é o input curado).
- NÃO implementar S-A1 (soft de distribuição entre não-obrigatórias) nesta sessão — fora do escopo, vira frente própria.
- NÃO mergear em main sem aprovação explícita do Bernardo (mesma regra do handoff anterior).
- Commit seletivo (`git add <arquivo>`, NUNCA `-A`). Antes de commitar: `git status` + `git diff --cached`. Verificar sessões paralelas em git status (sessão E.0 teve incidente com `banco_exercicios.xlsx` aparecendo "modified" sem ter sido tocado — restored).
- NÃO use `--no-verify` ou flags que pulem hooks.
- Se ambiguidade aparecer fora dos 4 pontos pendentes listados, pergunte (AskUserQuestion) antes de chutar.
- **Validação obrigatória pré-merge**: rodar `tools/harness_comparativo_e0.py --n 100 --out docs/refatoracao/relatorios/E0_<data>_pos_h_a1.md` e confirmar que (a) violação `ombro_composto` cai de 100% pra ≤ esperado pelo graceful degradation; (b) violação `biceps` em `bracos(2)` cai pra 0%; (c) violação `hinge` em `perna_posterior(2)` cai pra 0%; (d) Wins do CSP da E.0 preservados (H-R1 costas continua 0% violação; overlap R-1 não regride).
